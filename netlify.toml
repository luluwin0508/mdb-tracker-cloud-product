[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 路由与定时任务都在各自的 v2 函数里用 `export const config` 声明：
# - api.mjs        → config.path = "/api/*"
# - refresh-*.mjs  → config.schedule（cron，UTC）
